# Sanitized live-read verification — 2026-08-20

This verification was performed on Windows with `codex-cli 0.147.0` and the folder-packaged `CodexQuotaGuard.exe` built from Python.org CPython 3.11.

## Procedure

The executable ran once with a new local data directory and `--once`. The hard-coded transport allowlist permitted only initialization and the two account reads. No prompt, task, thread, turn, response, reset-credit operation, or account write was issued.

## Sanitized result

```json
{
  "source": "Codex App Server",
  "status": "live",
  "rateLimitsRead": "succeeded",
  "weeklyWindow": "present",
  "fiveHourWindow": "not returned in this snapshot",
  "accountUsageSupported": true,
  "absoluteEstimate": "warming_up",
  "inferenceOperationsInvoked": false,
  "processExitCode": 0,
  "sqliteDatabaseBytes": 57344
}
```

The exact account percentage, reset timestamp, token total, account identifiers, and local paths are intentionally omitted from the repository. A new data directory contains only one percentage point, so the correct first-cycle output is **Calibrating / about 5 percentage points required**. No usage was generated to accelerate calibration.

## Packaging verification

- All 34 automated tests passed before the build.
- SQLite loaded in the packaged executable; the earlier Anaconda `_sqlite3` DLL failure was removed by building from standalone CPython.
- The Qt package includes Windows platform and allowlisted QML modules.
- Default, 903-pixel-wide, and maximized Windows layouts were inspected.
- Closing the window left the exact packaged process running in the Windows tray.
